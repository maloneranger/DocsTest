/// The interface to the whole package
public struct Interface {
    /// This function, when called, does the thing that this package is intended for.
    public func doTheMagic() {
        print("Doing Magic")
    }
}
